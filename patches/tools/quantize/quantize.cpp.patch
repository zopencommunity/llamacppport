diff --git i/tools/quantize/quantize.cpp w/tools/quantize/quantize.cpp
index 0a48332..cb6ba7b 100644
--- i/tools/quantize/quantize.cpp
+++ w/tools/quantize/quantize.cpp
@@ -251,6 +251,7 @@ static int load_imatrix(const std::string & imatrix_file, std::vector<std::strin
     struct gguf_init_params meta_gguf_params = {
         /* .no_alloc = */ false, // the data is needed
         /* .ctx      = */ &ctx,
+	/* .allow_byteswapping = */ true,
     };
     struct gguf_context * ctx_gguf = gguf_init_from_file(imatrix_file.c_str(), meta_gguf_params);
     if (!ctx_gguf) {
